/* Crea una función que devuelva el número PI con dos decimales.
 Utiliza la variable PI que ya existe en javascript */

 function numero (){
    pi =  Math.PI;
    pii = pi.toFixed(2);
    return pii;
 }
 numero();
 console.log(pii);